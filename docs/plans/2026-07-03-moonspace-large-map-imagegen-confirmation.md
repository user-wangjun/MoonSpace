# MoonSpace Large Map Imagegen Confirmation Notes

## Current Confirmation State

Runtime assets have not been replaced yet.

The user has approved the large-map direction conceptually, but formal replacement still needs an explicit confirmation before copying assets into `assets/sprites/moonspace/` or changing gameplay rendering to use the large-map camera.

## Selected Candidate Inputs

- Large clean gameplay background:
  - `assets/source/imagegen/candidates/candidate_gameplay_courtyard_clean_bg_horror_v4.png`
- Main menu background:
  - `assets/source/imagegen/candidates/candidate_main_menu_scene_horror_v3.png`
- Opening CG storyboard:
  - `assets/source/imagegen/candidates/candidate_opening_cg_storyboard_horror_v3.png`
- Player 16-frame sheet:
  - `assets/source/imagegen/candidates/candidate_player_envoy_16frames_horror_v6.png`
- Wugang 16-frame sheet:
  - `assets/source/imagegen/candidates/candidate_wugang_16frames_horror_v7.png`
- Yutu 16-frame sheet:
  - `assets/source/imagegen/candidates/candidate_yutu_16frames_horror_v7.png`

## Staged Runtime Outputs

Generated by:

```powershell
.\.venv\Scripts\python.exe tools\stage_imagegen_v4_large_assets.py
```

Output directory:

```text
tmp/imagegen-v4-large-assets/assets/sprites/moonspace/
```

Expected staged files:

- `courtyard_bg_large.png` at `960x540`
- `main_menu_bg.png` at `480x270`
- `opening_cg_room.png` at `480x270`
- `opening_cg_moonlight.png` at `480x270`
- `opening_cg_blood_moon.png` at `480x270`
- `opening_cg_blackout.png` at `480x270`
- `player_envoy_large.png` at `168x248` as a 4x4 sheet of `42x62` frames
- `wugang_chop_large.png` at `304x368` as a 4x4 sheet of `76x92` frames
- `yutu_pounding_large.png` at `256x312` as a 4x4 sheet of `64x78` frames

## Dry Run Replacement Command

Safe dry run:

```powershell
.\.venv\Scripts\python.exe tools\apply_imagegen_large_assets.py --skip-stage
```

Actual asset replacement after explicit confirmation:

```powershell
.\.venv\Scripts\python.exe tools\apply_imagegen_large_assets.py --confirm-replace-runtime-assets
```

## Code Integration Needed After Confirmation

- Add large map dimensions to `config.py` while keeping the screen at `480x270`.
- Draw `courtyard_bg_large.png` as a world-sized background.
- Add camera follow logic in `core/game.py`.
- Pass camera offset based on player position, clamped to the large map bounds.
- Keep UI, dialog box, rule book, and status strip screen-fixed.
- Switch the player, Wugang, and Yutu draw methods to large 4x4 sprite sheets.
- Keep collision rectangles small and gameplay-stable while visual sprites are larger.
- Update NPC animation frame counts from 4 or 3 to 16-frame loops.
- Remove or gate old overlay drawing that duplicates new image content:
  - Main menu extra red moon ring.
  - Opening CG first-shot code-drawn computer overlay where it conflicts with the imagegen CG.

## Current Verification

Passing command:

```powershell
.\.venv\Scripts\python.exe -m pytest tests\test_imagegen_large_staging.py tests\test_assets.py tests\test_main_menu.py tests\test_opening_cg.py
```

Latest observed result: `15 passed`.

## Confirmation Gate

Do not replace runtime assets or integrate the large-map gameplay code until the user explicitly confirms the 16-frame large-map direction.
